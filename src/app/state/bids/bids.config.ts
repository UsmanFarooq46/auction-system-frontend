import { provideState } from '@ngrx/store';
import { provideEffects } from '@ngrx/effects';
// import { bidsFeature } from './bids.reducer';
// import { BidsEffects } from './bids.effects';

/**
 * Provides all NgRx dependencies for the Bids feature
 * @returns Array of providers for bids state management
 */
export const provideBidsFeature = () => [
//   provideState(bidsFeature),
//   provideEffects([BidsEffects])
];
